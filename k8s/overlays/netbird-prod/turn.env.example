# Copy to turn.env (gitignored) and fill in with a random 32-byte hex secret:
#   openssl rand -hex 32
#
# The same value MUST end up in both:
#   - the mgmt service (signs turnCredentials with it)
#   - the coturn --static-auth-secret
TURN_SHARED_SECRET=REPLACE_ME_openssl_rand_hex_32
