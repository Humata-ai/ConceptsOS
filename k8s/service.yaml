apiVersion: v1
kind: Service
metadata:
  name: conceptsos
  labels:
    app: conceptsos
spec:
  type: LoadBalancer
  selector:
    app: conceptsos
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
