# WireGuard server key + peer table (contains all client private keys).
# Never commit — treat this dir like a password vault. Back it up encrypted.
.wg-state/

# terraform local state / plans
terraform/.terraform/
terraform/*.tfstate*
terraform/*.tfplan
terraform/crash.log

# node
app/node_modules/
app/.next/
ChatUI/node_modules/
ChatUI/.next/
AgentChat/node_modules/
AgentChat/.next/
AgentChat/test-results/

# DesktopUI (Prozilla-OS derived CRA app)
DesktopUI/node_modules/
DesktopUI/build/

# api service (Next.js)
api/node_modules/
api/.next/
api/.env.local
api/.env
api/*.tsbuildinfo

# wg-gateway
docker/wg-gateway/node_modules/

# supabase local
supabase/.temp/
